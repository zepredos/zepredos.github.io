# zep's website

A static Vite and TypeScript portfolio with a GRUB-inspired entry screen, projects, technical writing, and work experience.

## Development

```bash
npm install
npm run dev
npm run build
npm run preview
```

Content lives in `src/content/`; shared site details are in `src/site.ts`.
